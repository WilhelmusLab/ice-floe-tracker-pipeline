[template variables]
START="2019-03-19"
END="2019-03-20"
SATELLITES="aqua", "terra"
LOCATION="beaufort_sea"
CRS="EPSG:3413"
BBOX="-2112500.0,-262500.0,-2012500.0,-162500.0"
SCALE=250
CENTROID_LAT=71.02795656503653
CENTROID_LON=-129.11755440990464
MINFLOEAREA=350
MAXFLOEAREA=90000
PREPROCESSING="Lopez"  # "Lopez" or "Buckley"


# Install locations:
# - "Apptainer" - use apptainer to load an image from the docker registry on the fly
# - "Docker" - use docker to load an image from the docker registry on the fly
# - "Source" - download and build the application from source on the fly, e.g. using pipx
# - "Inject" - use a specific command

# Lopez Ice Floe Tracker settings
IFT_INSTALL="Docker"  # "Apptainer" or "Docker" or "Inject"
IFT_COMMAND=""  # Command to run the IFT CLI if using "Inject" install mode

# Buckley Floe Size Distribution preprocessing settings
FSDPROC_INSTALL="Source"  # "Source" or "Inject"
FSDPROC_COMMAND=""  # Command to run the FSDPROC CLI if using "Inject" install mode

# Satellite Overpass Identification Tool settings
PASS_TIME_INSTALL="Source"  # "Source" or "Inject"
PASS_TIME_COMMAND=""  # Command to run the Pass Time CLI if using "Inject" install mode
SPACEUSER=""  # Username for Space-Track.org
SPACEPSWD=""  # Password for Space-Track.org

# Floe colorizer settings
COLORIZE_INSTALL="Source"  # "Source" or "Inject"
COLORIZE_COMMAND=""  # Command to run the Pass Time CLI if using "Inject" install mode



